# Number Guessing Game in FlowLang

show "Welcome to the Number Guessing Game!"
show "I'm thinking of a number between 1 and 100. Can you guess it?"

let secret_number be random(1, 100)
let guess be 0
let attempts be 0

# while guess != secret_number do
#     show "Enter your guess:"
#     let guess be input()
#     let attempts be attempts + 1
#     if guess < secret_number then
#         show "Too low! Try again."
#     else
#         if guess > secret_number then
#             show "Too high! Try again."
#         else
#             show "Congratulations! You guessed the number in " + attempts + " attempts."
#         end
#     end
# end

def mad_lib with a, b, c do
    show "The " + a + " jumped over the " + b + " and " + c + "."
end

mad_lib("cat", "dog", "mouse")


