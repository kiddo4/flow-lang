# FizzBuzz in FlowLang
# Print numbers 1-30, but:
# - "Fizz" for multiples of 3
# - "Buzz" for multiples of 5  
# - "FizzBuzz" for multiples of both

show "FizzBuzz from 1 to 30:"
show ""

for i from 1 to 30 do
    if i % 15 == 0 then
        show "FizzBuzz"
    else
        if i % 3 == 0 then
            show "Fizz"
        else
            if i % 5 == 0 then
                show "Buzz"
            else
                show i
            end
        end
    end
end